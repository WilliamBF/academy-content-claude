{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c '[ -n \"${CONTENT_CREATION_PLUGIN_ROOT:-}\" ] && python \"${CONTENT_CREATION_PLUGIN_ROOT}/hooks/scripts/check_update.py\" || exit 0'",
            "timeout": 10,
            "statusMessage": "Checking for plugin updates..."
          }
        ]
      }
    ]
  }
}
